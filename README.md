# 动物识别系统

基于深度学习（CNN）和产生式系统的动物图像识别项目，支持 Animals-10 数据集的10种动物分类。

## 📋 项目概述

本项目实现了一个完整的动物识别系统，包含两个核心模块：

1. **CNN深度学习模块** - 使用ResNet50进行图像识别
2. **产生式系统模块** - 基于规则的推理引擎，用于动物特征分类

### 支持的动物类别（Animals-10）

- 🐕 狗 (cane)
- 🐱 猫 (gatto)
- 🐴 马 (cavallo)
- 🐄 牛 (mucca)
- 🦌 鹿 (cervo)
- 🐘 大象 (elefante)
- 🐿️ 松鼠 (scoiattolo)
- 🦋 蝴蝶 (farfalla)
- 🕷️ 蜘蛛 (ragno)
- 🐔 鸡 (gallina)

## 🚀 快速开始

### 环境要求

- Python 3.8+
- CUDA 支持（可选，用于GPU加速）

### 安装依赖

```bash
pip install -r requirements.txt
```

主要依赖包括：
- PyTorch 2.0.1
- torchvision 0.15.2
- numpy, pandas, matplotlib
- scikit-learn
- colorama (用于产生式系统)

### 数据准备

1. **下载 Animals-10 数据集**：
   ```bash
   cd Data
   python download_dataset.py
   ```

2. **处理数据**：
   ```bash
   python prepare_data.py
   ```

数据集结构：
```
Data/split_dataset/
├── train/          # 训练集
│   ├── cane/
│   ├── cavallo/
│   └── ...
├── val/            # 验证集
│   ├── cane/
│   ├── cavallo/
│   └── ...
└── test/           # 测试集
    ├── cane/
    ├── cavallo/
    └── ...
```

## 🎯 使用方法

### 1. CNN模型训练

**训练 ResNet50**：

```bash
python CNN_system/Resnet50_CNN.py
```

#### 训练参数说明

- `--model`: 模型架构 (resnet50)
- `--epochs`: 训练轮数
- `--batch_size`: 批次大小
- `--lr`: 学习率
- `--num_workers`: 数据加载进程数
- `--data_dir`: 数据集路径

### 2. CNN模型预测

使用训练好的模型进行预测：

```bash
python CNN_system/test_model.py
```

### 3. 产生式系统演示

运行基于规则的动物识别系统：

```bash
python Production_system/Production_system.py
```

## 📁 项目结构

```
动物识别_毛灿/
├── README.md                    # 项目说明文档
├── requirements.txt             # 依赖包列表
│
├── CNN_system/                  # CNN深度学习模块
│   ├── Resnet50_CNN.py          # CNN 模型训练脚本
│   ├── test_model.py            # 模型性能评估脚本（混淆矩阵 + 分类报告）
│   └── best_resnet50.pth        # 保存的最好模型
│
├── Production_system/           # 产生式系统模块
│   ├── Production_system.py     # 演示程序
│   └── rules_base.py            # 规则集
│
├── data/                        # 数据处理模块
│   ├── download_dataset.py      # 数据集下载
│   ├── prepare_data.py          # 数据预处理
│   ├── raw-img/                 # 原始图像数据目录
│   └── split_dataset/           # 处理图像数据目录    
```

## 📝 数据集信息

- **数据集名称**: Animals-10
- **来源**: Kaggle
- **总样本数**: 26,179 张图像
- **训练集**: 20,938 张 (~80%)
- **测试集**: 5,241 张 (~20%)
- **图像格式**: JPG
- **分辨率**: 变化（训练时resize到224x224）

